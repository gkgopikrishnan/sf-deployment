name: Deploy to Snowflake new

on:
  push:
    branches:
      - main  # or your preferred branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SNOWSQL_ACCOUNT: SPHEUCZ-HQ66596
      SNOWSQL_USER: SFTRAINING
      SNOWSQL_PASSWORD: GoodSuperluck2025
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print env and PATH 1
        run: |
          echo "PATH before Snowflake CLI install:"
          echo $PATH
          env
          
      - name: Install Python and pipx
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install --user pipx
          python3 -m pipx ensurepath

      # Add pipx binaries path so snowsql can be found in next steps
      - name: Add pipx binary path to environment
        run: echo "${HOME}/.local/bin" >> $GITHUB_PATH

      - name: Install Snowflake CLI manually via pipx
        run: ~/.local/bin/pipx install --force snowflake-cli

      - name: Verify Snowflake CLI installation
        run: |
          which snowsql
          snowsql -v

      - name: Deploy SQL scripts to Snowflake
        run: snowsql -f sql/deploy.sql  # Adjust path if necessary


      - name: Deploy SQL scripts
        run: snowsql -f sql/deploy.sql   # adjust the path if needed
